# dependencies
node_modules
.pnp
.pnp.js

# production
dist
dist-ssr
*.local

# env / secrets
.env
.env.local
.env.*.local

# editors
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS
.DS_Store
Thumbs.db

# TypeScript incremental build cache
*.tsbuildinfo


# Cost probe reports (per-run JSON dumps)
tools/cost-probe-reports/

# Private / internal — never push (monetization dashboards, draft pricing docs, etc.)
private/

# Session handoff context (gitignored — local-only Tony bootstrap doc)
docs/SESSION-CONTEXT.md
